import '@testing-library/jest-dom/extend-expect';

import { createUser } from './utils/api';

// TODO move this function into each integration test
//  also fix the integration tests by doing `await waitFor` in all of the tests
// createUser();
